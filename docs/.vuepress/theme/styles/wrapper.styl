$wrapper
  max-width $contentWidth
  margin 0 auto
  padding 2rem 2.5rem

//  @media (max-width: $MQMobileNarrow)
//  @media (max-width: $MQMobile)
  @media (max-width: 999px)
    margin 0 auto
    padding 1.5rem
    div.content
      margin 0 auto
      max-width 100%

  @media (min-width: 1000px)
    margin 0 auto
    padding 2rem 2.5rem
    div.content
      margin 0 auto
      max-width 60%

.theme-default-content:not(.custom)
    margin 0 auto
    max-width 100%

div
  .content > h2
    margin 0 auto
    text-align center
  
  .content > h3
    margin 0 auto
    text-align center

  .content > h4
    margin 0 auto
    text-align center

  .content > h5
    margin 0 auto
    text-align center



p.dc::first-letter
  line-height 1
  font-size 3em
  float left


p.dia
  text-align  justify
  text-justify  auto
  display inline-block
  text-indent -1em

span.emphasis
  text-emphasis filled dot

table
  border-collapse collapse
  margin 1rem 0
  display block
  overflow-x scroll
  white-space nowrap
  -webkit-overflow-scrolling   touch


//------------------------------------------------------------------------------------------

/*踊り字を横書き用に回転する*/
span.rotate_odori1 {
  display: inline-block;
  transform: rotate(-90deg) scale(-1, 1);
  height: 90%;
}
span.rotate_odori2 {
  display: inline-block;
  transform: rotate(-90deg) scale(-1, 1);
}

/*"―"の二倍化
  二つ並べるとつながらないフォント用。
  ～を伸ばすのにも使う。
*/
span.lngx {
  display: inline-block;
  transform-origin: left;
  transform: scale(1.5, 1) translateX(3px);
  margin-right: 1em;
}

/*会話をぶら下げインデントにする。
  注:本文から"「","」"を除去し、下記のクラスを適用する必要がある。
  ぶら下げ表現をtext-indentで実装には  inline-blockにする必要があるが
  inline-blockにすると間で改ページできなくなるため不可。
*/
span.talk::before{
  content: "「";
  margin-left: -1em;
}
span.talk::after{
  content: "」";
}

span.think::before{
  content: "（";
  margin-left: -1em;
}
span.think::after{
  content: "）";
}

/*部分的に縦書き。ここでは中寄せ*/
div.prtlyV{
  margin-top: -10mm;
  padding-top: 10mm;
  display: flex;
  width: 100%;
  height: 100mm;
  font-size: 1.2em;
  writing-mode: vertical-rl;
  align-items: center;
}

/*ラストの「fin」専用右下*/
#この世界の一部では、ないことを div.right {
  position: absolute;
  bottom: 0mm;
  right: 0mm;
}